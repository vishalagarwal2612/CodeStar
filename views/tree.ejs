<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Trees | CodeStar</title>
  <link rel="stylesheet" href="/array_style.css">
</head>
<body>
  <!-- Navbar -->
<header class="navbar">
  <div class="logo">⚡ CodeStar</div>

  <% if (user) { %>
    <!-- Logged in: show nav links + logout -->
    <nav class="nav-links">
      <a href="/">Home</a>
      <a href="/problems">Problems</a>
      <a href="/profile">Profile</a>
      <a href="/leaderboard">Leaderboard</a>
    </nav>
    <div class="auth-buttons">
      <a href="/logout" class="btn-primary">Logout</a>
    </div>
  <% } else { %>
    <!-- Not logged in: show only login/signup -->
    <div class="auth-buttons">
      <a href="/login" class="btn-outline">Login</a>
      <a href="/signup" class="btn-primary">Sign Up</a>
    </div>
  <% } %>
</header>


  <!-- Tree Topic Hero -->
  <section class="topic-hero">
    <h1>Trees</h1>
    <p>Explore Tree problems by sub-categories for structured learning.</p>
  </section>

  <!-- Subcategories -->
  <section class="subcategories">
    <div class="subcategory-grid">
      <% const categories = [
            { name: "Binary Tree", desc: "Practice fundamental tree traversals, recursion, and structural problems." },
            { name: "Segment Tree", desc: "Master range queries, lazy propagation, and interval problems." },
            { name: "Miscellaneous", desc: "Tackle a variety of important problems that don’t fit standard categories." }
        ]; %>
      <% categories.forEach(category => { %>
        <a href="/problems/trees/<%= category.name.replace(/\s+/g, '-').toLowerCase() %>" 
           class="subcategory-card">
          <h3><%= category.name %></h3>
          <p><%= category.desc %></p>
        </a>
      <% }) %>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <p>© <%= new Date().getFullYear() %> CodeStar. Built for coders, by coders.</p>
  </footer>
</body>
</html>
